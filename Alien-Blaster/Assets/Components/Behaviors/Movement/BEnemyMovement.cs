using UnityEngine;

public class BEnemyMovement : MonoBehaviour
{
    // Right now this does nothing, but there will be arrays of scripted movements that are executed sequentially.
    // Currently this is here just to give the missiles something to target.
}
